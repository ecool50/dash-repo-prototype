name = "dash-embed-query"
main = "embed-query.js"
compatibility_date = "2026-01-01"

[vars]
ALLOWED_ORIGIN = "https://ecool50.github.io"

# Cloudflare Workers AI binding — gives env.AI.run(...) inside the Worker.
# No external API key required.
[ai]
binding = "AI"

name = "dash-frontend"
compatibility_date = "2026-04-01"

# Serve the Vite-built static bundle from dist/.
# SPA mode: if a request doesn't match a file, return index.html so
# client-side routing in React works.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
